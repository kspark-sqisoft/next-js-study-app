# 이 파일을 복사해 .env 를 만드세요:  cp .env.example .env
# SQLite DB 파일 경로 (프로젝트 루트 기준 상대 경로 또는 절대 경로)
DATABASE_PATH=./data/app.db

# 세션 쿠키(JWT) 서명 키. 아래 명령으로 생성해서 넣으세요:  openssl rand -base64 32
SESSION_SECRET=change-me-generate-with-openssl-rand-base64-32
